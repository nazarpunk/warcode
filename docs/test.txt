function RMinBJ takes real a, real b returns real
    if a < b then // 3
        // 1
        // 2
        // 3
        return 224
    endif //dfdfdff
    // ssscscsc

    if ((a < b)) then // 3
        // 1
        // 2
        // 3
    endif

    return 33
endfunction
